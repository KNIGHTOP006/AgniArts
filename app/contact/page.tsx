import { EnquiryForm } from "../../components/EnquiryForm";
import { PageHero } from "../../components/PageHero";

export default async function ContactPage({ searchParams }: { searchParams: Promise<{ type?: string }> }) { const params = await searchParams; const isPerformance = params.type === "performance"; return <main><PageHero label="10 — START A CONVERSATION" title={isPerformance ? "Invite Agni." : "Be part of the rhythm."} text={isPerformance ? "Bring Agni Arts to your next gathering, event or cultural program." : "Want to learn, collaborate or simply know more? Get in touch."} image="/media/4.jpg" /><section className="form-section"><div className="container form-layout"><div><p className="eyebrow">{isPerformance ? "PERFORMANCE BOOKING" : "GET IN TOUCH"}</p><h2>{isPerformance ? "Make a performance enquiry." : "Start a conversation."}</h2><p>{isPerformance ? "Share the details of your event and we will get back to you." : "For workshops, collaborations and cultural projects, leave us a message."}</p></div><EnquiryForm type={isPerformance ? "performance" : "workshop"} /></div></section></main>; }
